
# .gitignore files specifies files and folders that should be ignore

#create a repository git init

#to create snapshots git add -A 
git commit -m"any massage"

#to check the status 
git log
#use the q to close the log
to add a remote
#git remote add origin <URL>

#push the first time
git puss -u origin master
#push the nth time 
git push

# install a virtual env tool

python3 -m pip install virtualenv

 

# create a virtual environment

python3 -m venv venv

 

 

# activate the virtual environment

# windows:

venv\Scripts\activate

 

# mac:

source venv/bin/activate

#to start flask app
activate venv/bin/activate
then flask --app server run --debug
and you will start the flask app